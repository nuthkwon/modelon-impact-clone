export function WorkspacePage(_props: { workspaceId: string; navigate: (path: string) => void }) {
  return <div style={{ padding: 24 }}>Workspace page — implemented by the app-shell module.</div>;
}
